# Test Spec — ClawPark OpenClaw Genome Pass

## Automated Verification
- `npm run test` passes
- `npm run lint` passes
- `npm run build` passes
- diagnostics show 0 errors

## New Coverage Targets
1. Breed result preserves identity/soul/skills/tools lineage info.
2. Demo mode still yields deterministic showcase flow.
3. Recursive lineage still works after model expansion.

## Manual QA Targets
- User can understand what SOUL / IDENTITY / SKILL / TOOLS mean on screen
- Breed result makes dimension-level changes legible
- Jurassic Park-style lab framing improves storytelling without overloading text
